[package]
name = "candle-rocknpu"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Candle frontend adapter for RockNPU"

[dependencies]
candle-core = "0.11.0"
half = "2"
rocket-runtime = { path = "../../crates/rocket-runtime" }
rocknpu-ops = { path = "../../crates/rocknpu-ops" }
rocknpu-tensor = { path = "../../crates/rocknpu-tensor" }

[workspace]
