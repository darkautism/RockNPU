# RockNPU 架構與技術說明

給想了解內部運作、調整參數、重現效能數據或參與開發的人。一般使用請看 [README.md](README.md)。

- [定位與邊界](#定位與邊界)
- [執行流程](#執行流程)
- [工作如何分配到 NPU 與 CPU](#工作如何分配到-npu-與-cpu)
- [NPU 執行路徑細節](#npu-執行路徑細節)
- [為什麼生成預設交給 CPU](#為什麼生成預設交給-cpu)
- [設定參數](#設定參數)
- [效能與量測方法](#效能與量測方法)
- [NPU 頻率](#npu-頻率)
- [系統調校](#系統調校)
- [開發者指南](#開發者指南)

## 定位與邊界

RockNPU 是 RK3588 NPU 的 **使用者空間（userspace）中間層**：

```
llama.cpp / Ollama / Candle / ONNX（前端，不修改）
          │  GGML 動態後端 / C ABI
          ▼
      RockNPU（本專案：切分、排版、量化、常駐權重、regcmd 產生、排程）
          │  /dev/accel/accel0（DRM accel 介面）
          ▼
   Linux rocket NPU 驅動（系統空間，不屬於本專案）
```

RockNPU 負責：前端轉接、與框架無關的 IR、張量與權重排版、RK3588 運算下放、暫存器指令（regcmd）合成、常駐權重與暫存區、量化、使用者空間排程、正確性與效能驗證。

RockNPU **不修改** 核心驅動，也 **不修改** 前端原始碼。驅動沒提供的能力，就維持不支援，而不是變成系統空間子專案。

## 執行流程

```
GGML 圖（llama.cpp 建構）
  └─ adapters/ggml-rocknpu（libggml-rocknpu.so）
        supports_op：決定哪些 MUL_MAT 交給 NPU
        graph_compute：同輸入投影分組、呼叫 C ABI
  └─ crates/rocknpu-capi（C ABI，Rust）
        量化活化值、常駐權重快取、M-tile 切塊、輸出重新縮放
  └─ crates/rocknpu-matmul
        Int8DecodePool：三個 NPU 核心各一個裝置 fd，由呼叫端執行緒直接送出
        Int8DecodeExecutor：常駐權重排版、regcmd 快取、提交/等待
  └─ crates/rocknpu-regcmd：INT8 M=1 / M-tile regcmd 編碼
  └─ crates/rocket-runtime / rocket-uapi：BO 配置、提交、同步
```

各 crate 的責任見 [docs/architecture.md](docs/architecture.md)。

## 工作如何分配到 NPU 與 CPU

| 工作 | 預設位置 | 說明 |
|---|---|---|
| 讀提示詞的投影層（Q/K/V/O、gate/up/down，M>1） | **NPU**，W8A8 M-tile | 計算密集，NPU 比 CPU 快約 7 倍 |
| 批次生成（多個請求同時生成，M 2–128） | **NPU**，W8A8 M-tile | 列數不足 tile 高度時補零 |
| 單序列生成（M=1） | **CPU**（`ROCKNPU_DECODE=cpu`） | 受記憶體頻寬限制，見下節 |
| 注意力、RMSNorm、RoPE、SwiGLU、殘差 | CPU | llama.cpp 原生實作 |
| 輸出層（詞彙表 N=32000） | CPU | W8 精度不足，且超過單一 tile 的 N 上限 |

## NPU 執行路徑細節

**權重**
- GGUF Q4_K/Q6_K 在第一次使用時反量化，再轉成「每輸出通道對稱 int8」（`scale = max|row| / 127`，四捨五入遠離零），排成 RK3588 原生 32×32 區塊後常駐在 NPU 可存取的 BO。
- 轉換在程序內完成；`ROCKNPU_W8_SIDECAR_DIR` 指向的 sidecar（`scripts/make_tinyllama_w8_sidecar_gguf.py` 產生）只是啟動快取，數值完全相同（KL 測試逐位元一致）。

**活化值**：每個 token（每列）各自對稱 int8 量化，輸出以 `int32 × 列 scale × 通道 scale` 還原成 f32。

**直接提交（direct submit）**：rocket 驅動對同一個 fd 的工作只會排到一顆 NPU 核心，所以 RockNPU 為三顆核心各開一個 fd。每次投影：
1. 每顆核心一個執行緒：把量化後的活化值寫入該核心的輸入 BO → `FINI_BO` → `SUBMIT`；
2. 等待該核心輸出 BO（`PREP_BO`）；
3. 直接從映射的輸出 BO 讀 int32，乘上 scale 寫進前端的 f32 輸出（不經中介緩衝）。

每個常駐權重的 regcmd 只編碼一次並重播；省略沒有必要的 `PREP_BO`/`FINI_BO`（CPU 只寫的輸入、CPU 只讀的輸出）。

**M-tile**：一次任務處理 M∈{4,8,12,16,32,48,64,128} 列（M>64 時每顆核心的 K≤2048）。任意 M 會切成 128 列的 tile 加上補零的尾端 tile，所以 llama.cpp 預設的 512 token ubatch、任意長度的提示詞都走 NPU。

**形狀一般化**（`split_wide_mtile`、`execute_padded_k_mtile`）：
- 128 列 tile 若每核心的 K 會超過 2048，改成兩個 64 列的半 tile；
- N>8192（例如 Qwen2.5 的 FFN 8960）切成每段 ≤8192 的欄塊；
- K 是 256 的倍數但不是 512 的倍數（Qwen2.5 FFN down 8960）時，權重與活化值都補零到 512 的倍數（精確）；
- N≥768 的投影把 N 分給三顆核心，較窄的在一顆核心上執行。

**K 切分**：K>4096 的投影（TinyLlama FFN down 5632、Llama 3.x 8192，上限 12288）由三顆核心各算一段 K（以 512 為單位），再逐列加總。

**同輸入分組**：同一個 GGML 子圖中讀同一份活化值的投影（V/K、gate/up）把權重沿 N 串接成一個常駐矩陣，一次量化、一次送出。

**跨子圖 Q+K+V**：Q、K、V 常落在不同的 GGML 子圖（Qwen 的 bias 加法在 CPU 上、或 V/K 分組排在 Q 之後），所以某層的 K/V 權重出現過一次後，Q 節點就把 Q、K、V 串接成一次 M-tile 呼叫，K/V 的結果暫存給之後的 K/V 節點（活化值指標與列數相同才使用）。第一次成功後以 `rocknpu_mtile_release()` 釋放不再使用的單一/成對快取，記憶體與之前相同；輸出與分開計算逐位元一致（活化值量化一次、scale 相同）。`ROCKNPU_MTILE_QKV=0` 可關閉。

**混合生成（`ROCKNPU_DECODE=hybrid`）**：M=1 投影的前一段輸出列由 NPU 以 W8 計算，其餘由 llama.cpp 的 CPU 核心以原始 GGUF 計算；CPU 部分透過 `rocknpu_context_set_overlap()` 在 NPU 送出後、等待前於同一執行緒執行。

## 為什麼生成預設交給 CPU

單序列生成每個 token 都要讀過一次全部權重，是記憶體頻寬問題：

| 路徑 | 每 token 讀取量 | 實測 |
|---|---:|---:|
| CPU Q4_K（llama.cpp，4×A76） | ≈ 0.64 GB | 32–33 tok/s（CPU 本身是算力瓶頸，隨執行緒數線性成長） |
| NPU W8（三核心直接提交） | ≈ 0.92 GB + 每次投影約 0.12 ms 固定開銷 × 88 次 | ≈ 20–21 tok/s |
| 混合（NPU 30% / CPU 70%） | 兩者相加，互搶 DRAM 頻寬 | ≈ 26 tok/s |

W8 權重比 Q4_K 多將近一倍的位元組，NPU 單核 DMA 約 9 GB/s、三核約 25–30 GB/s，再加上驅動每個工作的固定開銷，NPU 生成追不上 CPU 的 4-bit 路徑。混合模式雖然讓兩邊同時工作，但 CPU 部分在 NPU 大量讀取時會慢約 1.4 倍（序列/重疊對照實驗），整體仍輸給純 CPU。因此預設 `cpu`；`npu` 適合想把 CPU 讓給其他工作的情境。

NPU 真正的強項是計算密集的提示詞處理與批次生成：M=128 時 NPU 一次讀取權重可服務 128 列。

## 設定參數

一般不需要設定。`rocknpu.env`（安裝腳本產生）內容：

| 變數 | 值 | 作用 |
|---|---|---|
| `GGML_BACKEND_PATH` | `…/libggml-rocknpu.so` | 讓 llama.cpp / Ollama 載入 RockNPU |
| `LLAMA_ARG_DEVICE` | `ROCKNPU0` | 選用 NPU 裝置（等同 `--device ROCKNPU0`） |
| `LLAMA_ARG_REPACK` | `false` | 保留 GGUF 原始權重排列（等同 `--no-repack`），否則 NPU 讀不到權重 |
| `GOMP_SPINCOUNT` | `20000` | 縮短 llama.cpp OpenMP 執行緒的忙等；預設值會讓閒置的 CPU 執行緒搶走 NPU 主機端工作的核心（pp128 496→551 tok/s，CPU 生成不受影響） |

RockNPU 自身參數（皆可選）：

| 變數 | 預設 | 說明 |
|---|---|---|
| `ROCKNPU_DECODE` | `cpu` | 單序列生成位置：`cpu` / `npu` / `hybrid` |
| `ROCKNPU_HYBRID` | 0.3（hybrid 模式） | 混合模式 NPU 負責的輸出列比例；`ROCKNPU_HYBRID_QKV/_O/_GATEUP/_DOWN` 可個別設定 |
| `ROCKNPU_HYBRID_THREADS` | 4 | 混合模式 CPU 部分的執行緒數 |
| `ROCKNPU_W8_SIDECAR_DIR` | 未設定 | 預先轉好的 W8 sidecar 目錄（僅加速啟動） |
| `ROCKNPU_W8_CONVERT` | 1 | 沒有 sidecar 時於程序內轉換；0 = 只接受 sidecar |
| `ROCKNPU_MTILE_GROUP` | 1 | 同輸入投影分組 |
| `ROCKNPU_MTILE_QKV` | 1 | 讀提示詞時跨子圖把 Q、K、V 合成一次 NPU 呼叫 |
| `ROCKNPU_PREFILL_HILO` | 0 | 讀提示詞時把活化值拆成 int8 + int8 殘差兩列（約 15 位元精度）：KLD 約降為 1/4–1/5，速度約減半；`down` = 只用在 FFN down（K>4096 或需補零的 K），速度約 −23 %。數據見「品質」一節 |
| `ROCKNPU_DISPATCH_SUMMARY` | 未設定 | 結束時印出 NPU 分派統計 |
| `ROCKNPU_M1_PROFILE` / `ROCKNPU_MTILE_PROFILE` / `ROCKNPU_SPLIT_PROFILE` | 未設定 | 各路徑的時間剖析 |

以下已驗證路徑預設開啟，可設為 `0` 關閉以比較或除錯：`ROCKNPU_W8_DIRECT_SUBMIT`、`ROCKNPU_EXPERIMENT_DIRECT_SCRATCH`、`ROCKNPU_PREFILL_CACHE`、`ROCKNPU_NATIVE_MTILE`、`ROCKNPU_W8_MTILE`、`ROCKNPU_MTILE_PERSIST`、`ROCKNPU_MTILE_MC`、`ROCKNPU_NATIVE_MTILE_DOWN`。

## 效能與量測方法

環境：Orange Pi 5（RK3588，LPDDR4X-2112）×2（8 GB「o8」、16 GB「o16」），Armbian 6.18 `current` 核心，rocket 驅動 + devfreq 模組（NPU 700 MHz），llama.cpp `391fac1`（b10969）以 `GGML_NATIVE=ON`、`GGML_CPU_REPACK=OFF` 建置，TinyLlama‑1.1B‑Chat‑v1.0 Q4_K_M（SHA-256 `5c66751b…`），4 個 A76 執行緒，`taskset -c 4-7`，`-fa on`。

```sh
. ~/.local/share/rocknpu/rocknpu.env
taskset -c 4-7 llama-bench -m model.gguf -t 4 -fa on -dev ROCKNPU0 -p 128,512 -n 64 -r 3
```

### 結果（2026-09-26，本分支）

| 設定 | pp128 | pp512 | tg64 |
|---|---:|---:|---:|
| CPU（llama.cpp，repack 開啟） | 73 | 69 | 33.3 |
| CPU（llama.cpp，repack 關閉） | 43 | 41 | 32–33 |
| RockNPU 起點（本日前 main 預設） | 19 | 37 | 13.0 |
| RockNPU 起點 + 當時最佳旗標 | 344 | 281 | 17.9 |
| **RockNPU 本分支預設 + `GOMP_SPINCOUNT=20000`** | **≈ 570** | **≈ 420** | **32–33（CPU 生成）** |
| 同上，`ROCKNPU_DECODE=npu` | ≈ 494 | ≈ 379 | ≈ 20 |
| 同上，`ROCKNPU_DECODE=hybrid` | ≈ 495 | ≈ 383 | ≈ 26 |

各項改進的貢獻（pp128，同一板子依序累加）：旗標預設化 116→344；直接提交 M-tile 344→433；同輸入分組 + 分段計時 433→435；每核心平行暫存與融合 rescale 435→486–496；`GOMP_SPINCOUNT` 496→551；任意 M 切塊讓預設 ubatch 512 也走 NPU（pp301/pp512 不再退回 FP16 路徑）；跨子圖 Q+K+V ≈ 556→572（Qwen2.5‑1.5B 324→350）。
生成（NPU 模式）：regcmd 重播與省略多餘同步 18.5→20.9 tok/s；CPU 深度 idle 關閉每個 NPU 工作省約 37 µs。

### 品質（KL divergence，wikitext-2 test，8×128 tokens，對照 CPU 參考 logits）

| 路徑 | Mean KLD | 與 CPU 相同 top-1 | PPL 比 |
|---|---:|---:|---:|
| 生成 `cpu`（預設） | 0 | 100 % | 1.000 |
| 生成 `hybrid`（NPU 40%） | 0.0099 | 97.0 % | 0.993 |
| 生成 `npu`（W8A8） | 0.0220 | 93.7 % | 1.035 |
| 提示詞處理（W8A8 M-tile） | 0.0232 | 93.1 % | 1.039 |

提示詞處理的新路徑（直接提交、平行暫存、融合 rescale、分組、切塊）與舊的執行緒路徑在 KL 上逐位元一致。

其他模型的提示詞處理（同樣量測方式，`-ub 128`；速度為 pp128 tok/s，o16）：

| 模型 | W8A8（預設） | `ROCKNPU_PREFILL_HILO=down` | `ROCKNPU_PREFILL_HILO=1` |
|---|---|---|---|
| TinyLlama‑1.1B | KLD 0.0232，top-1 93.1 %，556 | KLD 0.0165，94.8 %，433 | KLD 0.0043，98.0 %，266 |
| Llama‑3.2‑1B | KLD 0.0180，top-1 94.2 %，580 | KLD 0.0064，96.4 %，447 | KLD 0.0042，97.6 %，287 |
| Qwen2.5‑1.5B | KLD 0.0425，top-1 90.7 %，311 | KLD 0.0222，91.3 %，235 | KLD 0.0119，93.5 %，147 |

誤差主要來自每個 token 一個 scale 的 int8 活化值量化：離群值（Qwen 特別多，且每個 token 位置不同，不是固定幾個通道）讓其餘數值只剩少數量化階。`HILO` 把每列活化值的 int8 量化殘差當成額外一列一起送進同一個 int8 kernel，兩半輸出相加即得約 15 位元的活化值精度，代價是 NPU 列數加倍。`-ub 100`（補零尾端 tile）與 `-ub 128` 逐位元一致。

量測工具：`llama-perplexity -c 128 -b 128 -ub {1|128} --kl-divergence-base / --kl-divergence`（`-ub 1` 強制每個 token 走 M=1 生成路徑）。

## NPU 頻率

主線 rocket 驅動沒有 devfreq，NPU 停在開機時脈；[sky-rk3588/rk3588-npu-gpu](https://github.com/sky-rk3588/rk3588-npu-gpu) 類的 out-of-tree 模組可提供 200–1000 MHz（1 GHz 需把 NPU 電壓提高到 850 mV）。

實測（o8，同一板子切換頻率，本分支預設 + `GOMP_SPINCOUNT=20000`）：

| NPU 頻率 | pp128 | pp512 | NPU 生成（`ROCKNPU_DECODE=npu`） |
|---|---:|---:|---:|
| 200 MHz（主線開機時脈） | 326 | 270 | 8.2 |
| 700 MHz（800 mV，預設電壓） | 556 | 408 | 20.3 |
| 1 GHz（850 mV） | 與 700 MHz 相差 <2%（較早版本實測 pp128 342 vs 344，M=1 原語相差 <2%） | | |

200 → 700 MHz 值得（`scripts/rocknpu-tune.sh dvfs`）；700 MHz 以上瓶頸是 DRAM 與主機端開銷而非 NPU 運算，**不建議為了 LLM 冒險加壓超頻**。

## 系統調校

`scripts/rocknpu-tune.sh`（`apply`/`install` 只改執行期設定，`restore` 可還原；選用的 `dvfs` 會編譯並載入外部調頻模組，不改電壓也不寫入 `/lib/modules`）：

- NPU 三個中斷分別綁到小核 0/1/2，避開跑 llama.cpp 的大核；
- 關閉 `cpu-sleep` 深度 idle：喚醒延遲約 220 µs，每個 NPU 工作完成都要付一次（實測每工作省 ~37 µs）；
- CPU governor = performance；
- 若 NPU 有 devfreq，調到目前電壓允許的最高頻率。

## 開發者指南

```sh
cargo build --release --workspace
cargo test --release --workspace
cmake -S adapters/ggml-rocknpu -B target/ggml-rocknpu -DCMAKE_BUILD_TYPE=Release -DGGML_SOURCE_DIR=/path/to/llama.cpp/ggml
cmake --build target/ggml-rocknpu -j
```

硬體閘門（需 `/dev/accel/accel0`）：

```sh
cargo run --release -p rocket-smoke                              # BO/提交/regcmd 冒煙測試
cargo run --release -p rocket-smoke --bin int8_decode_m1 -- 2048 2048
cargo run --release -p rocket-smoke --bin int8_decode_multicore -- 2048 5632 3
cargo run --release -p rocket-smoke --bin int8_mtile -- 128
cargo run --release -p rocket-smoke --bin fused_residual -- 16 2048 2048
```

完整驗證矩陣見 [docs/repro.md](docs/repro.md)。

**正確性原則**：每條升級為預設的 NPU 路徑都需要獨立的參考（原語用 CPU 數學參考、模型用 llama.cpp CPU logits 的 KL / 貪婪輸出比對）；兩條 RockNPU 自己的路徑互相比對不算證據。

**效能原則**：局部原語變快不等於整體模型變快；小幅改進要用同程序或交錯 A/B，記錄模型雜湊、前端版本、環境變數、CPU/NPU 頻率。

**Workspace**

```
rocknpu           高階 Graph/Executable/Session API
rocknpu-capi      給外部轉接層的 C ABI
rocknpu-ir        與前端無關的圖/張量描述
rocknpu-llm       Transformer 執行元件
rocket-uapi       accel 裝置 ABI 包裝
rocket-runtime    安全的裝置/BO/提交層
rocknpu-regcmd    RK3588 regcmd 編碼
rocknpu-conv      Conv2D
rocknpu-matmul    MatMul / 生成 / M-tile / 常駐權重 / 核心池
rocknpu-tensor    張量契約
rocknpu-ops       運算子分派
rocknpu-onnx      ONNX 匯入與參考執行
rocket-smoke      實機閘門
adapters/ggml-rocknpu   llama.cpp / Ollama 動態後端
adapters/candle-rocknpu Candle 轉接層
```

**與框架無關的 API**

```rust
use rocknpu::{Executable, Graph, Session};
let graph: Graph = frontend.lower_to_rocknpu()?;
let session = Session::from_executable(Executable::compile(graph)?)?;
let output = session.run(input)?;
```

**文件索引**：[docs/architecture.md](docs/architecture.md)（crate 責任）、[docs/research-status.md](docs/research-status.md)（研究方向與假說）、[trialanderror.md](trialanderror.md)（已驗證/已否決實驗）、[docs/repro.md](docs/repro.md)（重現步驟）、`docs/benchmarks/`（歷史原始數據）。
